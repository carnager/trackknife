# Feature matrix

This matrix describes intended scope rather than claiming completion. M0
through M4 are complete; M5 — Trackbench's fast tag workspace and safe file
operations — is active. ADR-0009 records the MPD-client-first product
direction, ADR-0010 defines queue/list tabs, ADR-0020 rejects a mixed playback
queue, and ADR-0025 splits the product into the **Trackknife** MPD client and
the **Trackbench** local workstation over shared libraries. Milestone numbers
follow the restructured `MILESTONES.md` (tagging M5, MusicBrainz M6,
ReplayGain M7, converter M8, Melody endpoint M9, release M10).

## Foundations

| Capability | Status | Required behavior |
| --- | --- | --- |
| C++23/Qt 6 skeleton | Complete | Qt-free core, strict builds/tests, virtualized UI spike, measured budgets. |
| `tkfmt-1` | Complete | Versioned pure compiled expressions shared by views, sorts, groups, transforms, and paths. |
| Source/reference model | M2 (complete) | MPD URI, remote snapshot, optional mapped raw path, and local-only source stay distinct. |
| MPD session | M2 (complete) | Command/idle workers, typed snapshots and query payloads, selective refresh, bounded autonomous reconnect, ambiguous-mutation non-replay, transport/output commands, stable-ID queue primitives, versioned queue diffs, bounded search/browse reads, and exactly-once stored-playlist reads/mutations exist. |
| Working-list model | M3 (complete) | Explicit live queue, server playlist, scratch, and saved kinds exist. Lists may retain remote and local references as working memory, but no mixed playback-queue kind is planned. |
| Job system | M4–M7 | ADR-0047 provides the first Qt-free bounded metadata mutation pool with serialized progress, cancellation, and ordered per-source results. ADR-0063 adds the matching file-publication pool with fresh admission, same/cross-filesystem dispatch, partial results, and shared-directory coordination. Shared resource-class scheduling and generalized retry/history remain. |
| Metadata abstraction | M2/M5 (native-FLAC baseline) | ADR-0033 provides Qt-free ordered arbitrary values, native/canonical field identity, qualifiers, explicit provenance/precedence, typed MusicBrainz identities/credits, raw-path source revisions, and a bounded TagLib reader. ADR-0043 qualifies exact native-FLAC text writes through a preservation-verified prepared copy. Other writers, artwork mutation, and loudness remain M5–M7 work. |
| Safe mutation engine | M5 (native-FLAC Apply baseline) | ADR-0042 provides an immutable freshly revalidated plan and conflict preview; ADR-0044 adds a Qt-free per-source lock, durable SQLite journal, verified sibling publication, retained exact backup, rollback, post-publication reread, dependent-state callback, and conservative startup recovery. ADR-0045 supplies the idempotent provenance-aware all-occurrence list/source-cache transaction. ADR-0046 presents recovery and ambiguous evidence at startup and adds byte-exact crash-recoverable undo, explicit backup release, and fixed bounded retention. ADR-0047 exposes ready native-FLAC plans through a two-worker cancellable Apply job with durable workspace prerequisite, per-source progress/partial results, fresh-preview retry, visible-row refresh, and immediate history reload. The lifecycle will later be shared by artwork, files, loudness, and conversion. |

## Priority 1: MPD client (Trackknife) and local playback groundwork

| Area | Capability | Milestone | Required behavior |
| --- | --- | --- | --- |
| Connection | Profiles and reconnect | M2–M3 | TCP/Unix socket, session-only password, visible reconnect state, bounded backoff, generation-safe results, explicit first-run setup, multiple saved profiles, one-click switching, and selected-profile startup auto-connect exist. Secret-service-backed authenticated auto-connect remains deferred. |
| Protocol | Capabilities and idle | M2 | Discover commands/tagtypes; refresh affected subsystems; preserve unknown fields. |
| Library | Browse and search | M2–M4 | The Server tab is a real expandable library tree backed by a versioned editor for explicit `tkfmt-1` grouping, label, and sort expressions. Its default is album artist → chronological album (undated last) → disc only for multi-disc releases → tracks. Root tags load first; expanding one artist performs one bounded exact MPD query and refuses silent truncation above 10,000 tracks. Tree selection never replaces the center workspace: the current or hovered node reveals append/insert/replace controls, and a matching context menu includes working-list destinations. A permanent recursive filter strictly shrinks the tree to matching rows (labels for branches, labels plus descriptive tags for tracks; matched rows never drag their subtree in), auto-expands the results, and a debounced, bounded server-side search additionally reveals unloaded artist roots whose descendants match, auto-fetching them only while few roots remain visible. Two-line level-specific rows show artist placeholders and album counts, dated album covers with track count/duration, and numbered tracks with duration. Lazy roots retain arrows through filtering and defer first expansion until children arrive so it animates. Covers load serially without request bursts or UI-thread decode. Non-recursive folders and stored playlists remain adjacent actions. Live search uses an anchored non-modal panel that preserves the active tab; album results load at most 32 covers serially, while committed searches remain independent tabs. Richer query syntax remains deferred. |
| Metadata | MusicBrainz organization | M2–M3 | Preserve IDs/sort fields; release-aware grouping with deterministic fallback. |
| Playback | Remote transport | M2–M3 | Play/pause/stop/seek/prev/next, volume, repeat/random/single/consume, and advertised ReplayGain status/control cross the worker/controller boundary with immediate UI state and passed live compatibility validation. |
| Outputs | MPD outputs | M2–M3 | Additive enable/disable/toggle and output attributes. |
| Outputs | Melody extensions | M2–M3 | Online/primary/format/bitrate and advertised exclusive switch without stock-MPD regression. |
| Queue | Live MPD queue | M2–M4 | Stable song-ID play/add/delete/move/priority, ordered multi-select append/Add next command lists, duplicate occurrences, album-header group selection with bounded serial cover loading, bounded multi-select deletion/crop/priority, native clear, multi-selection drag reorder as one stable-ID command list, versioned reconciliation, and selection-preserving row changes exist. Priority is capability-gated and preserved in typed snapshots. Drag completion explicitly restores the source viewport, and a full-width labeled insertion marker uses the same album-aware resolved row delivered to the mutation callback; offscreen regressions cover both. M4 follow-up must still render nonzero priority and confirm the native multi-selection reorder against a live server end-to-end. Recoverable stale-ID rejections trigger authoritative queue/player reconciliation and an explicit non-modal conflict notice; richer concurrent-edit explanations remain. |
| Lists | Queue/list tabs | M3–M4 | In Trackknife, live queue, stored playlists, search results, persistent scratch tabs, and named working lists have distinct semantics; create/rename/pin/duplicate/reorder/close, dirty protection, keyboard switching, exact item edits, copy/move menus, cross-tab drag, crop, sort, reverse, randomize, and deduplicate are wired with mouse/keyboard actions. Trackbench's local working tabs now persist the matching rename, pin, duplicate, explicit-save, dirty-close, reorder, and restart lifecycle, while its rows retain local-only playback and file-work semantics. Its row context menu targets rows, preserved multi-selections, or album headers consistently and reuses Play/Remove plus cross-list Copy/Move behavior; folder context actions feed the same bounded intake path. ADR-0027 adds per-list semantic presentation state: grouped side/header artwork, plain columns, compact queue, and independently persisted column order/visibility/width. Custom `tkfmt-1` columns/group definitions remain. |
| Persistence | List documents and operation evidence | M3–M5 | Sixteen reversible SQLite migrations transactionally preserve scratch/saved kind, tab order, duplicate MPD/local references, raw path bytes, provenance-aware repeated snapshot fields and qualifiers, dirty/pinned state, remote profile identity, multiple connection profiles, opaque per-binding track-view payloads (client Qt header state; Trackbench's validated ADR-0027 JSON), distinct logical identity/physical source/decoder-selection/sample-range state for local subsongs, ADR-0044 metadata-operation journals with exact raw recovery paths and revisions, ADR-0045's verified local-source metadata cache/idempotency records, ADR-0046's separately guarded retained-backup lifecycle, ADR-0049–0053's normalized named metadata-transformation chains, ADR-0055's independently versioned naming layouts plus raw-byte destination roots, ADR-0056's exact source/target/prepared identities and same/cross-filesystem publication boundaries, ADR-0059's ordered revision-qualified all-occurrence source relocations, and ADR-0060's related reverse-publication attempts. A serialized worker owns ordinary list and dependent metadata/path state; startup loads and debounced saves do no SQL work on the UI thread, while shutdown performs a durable worker-thread flush. |
| Playlists | MPD stored playlists | M2–M3 | Typed discovery and metadata-rich loading open reusable, closable server-playlist tabs. Exactly-once create/save, queue-load, append/insert, multi-entry remove, reorder, clear, rename, and delete operations cross the adapter/session boundary. Advertised capabilities gate their tab/menu actions and accepted edits reload authoritative contents; richer dirty/conflict/recovery presentation remains. |
| UX | Complete default workspace | M3–M4 | Pure Qt Widgets shells preserve polished defaults. Per ADR-0025 the client's Library pane is MPD-only (server browsing/playlists); the Local Folders surface and filesystem navigation moved to Trackbench in the M4 split. Trackknife retains its native dock workspace. ADR-0026 gives Trackbench a versioned composed-panel tree: its real Folders and Track Lists panels ship side by side and may be switched to top/bottom or tabbed, reordered, reset, and restored with strict validation and forward-version preservation. ADR-0027 makes the Track Lists content compositional too: the default full-width album header and side cover sit beside normal configurable columns without changing row selection/drop semantics. Live/scratch/named/stored/search document tabs, transient live search, compact player chrome, context actions, and non-blocking failures retain one interaction language. |
| UX | Command palette/shortcuts | M3 | Major registered actions are searchable, runnable, conflict-checked, and assigned persisted shortcuts; tab/search/dialog focus rules and accessible names have regression coverage. |
| Performance | Fluid large/remote UI | M3–M4 | Virtualized bounded models, cancellable network requests, server-windowed continuation, worker-thread artwork decode, bounded art cache, and p50/p95 interaction measurements are implemented. Trackbench's allocation-free album-start role and fixed row geometry avoid `ResizeToContents` whole-model work; inserted pre-grouped rows (including resolved CUE tracks) defer their localized section override until Qt has finalized the vertical-header insertion, keeping the first track below the shared album header without a source-specific renderer. Its 100,000-row release corpus keeps grouped scrolling, selection/status, cached tabs, regrouping, and cached artwork updates inside the shared budgets, with an enforcing 10,000-row debug smoke. Live network, filesystem, and compositor latency remain outside these cached-interaction measurements. |
| Local sources | Preparation intake | M4 (complete baseline) | File/folder/drop/recent/CLI entries use one cancellable raw-path discovery path and enter persistent working lists without becoming MPD members. External `.cue` files resolve playable references strictly inside the sheet directory off the UI thread, create metadata-rich logical rows, and suppress a redundant whole-source row from the same intake batch. The **Local Folders** tab persists chosen raw-byte roots, lazily enumerates direct children off the UI thread, losslessly presents invalid UTF-8, and omits directory symlinks. Per ADR-0025 this entire surface now lives in Trackbench. Deeper root management and preparation-readiness badges may grow with M5 operations. |
| Local audition | FFmpeg + PipeWire | M4 (complete) | Qt-free FFmpeg probe and cancellable bounded source-rate float decode pass real PCM WAV fixtures. Sample-accurate seek/ranges and repository-owned AAC/M4A, LAME MP3, Opus/Ogg, Ogg Vorbis timestamp, tagged FLAC, tagged WavPack, tagged 24-bit big-endian AIFF, forced-small stereo 24-bit RF64, stereo float Wave64, chaptered Matroska/FLAC, and multi-subsong ProTracker fixtures prove metadata projection, exact manual gapless trim, selected-source playback, and a contiguous logical PCM timeline even when container frame timestamps overlap, leave gaps, or use a coarse time base. ADR-0028 carries bounded external-cue parsing through contained Trackbench intake, persisted logical identity, and segment-aware playback: raw path bytes survive, multi-file/data boundaries are explicit, shared 75 Hz boundaries map into adjacent sample ranges without drift, and consecutive logical tracks may gaplessly share one physical source. ADR-0031 projects exhaustive container-native chapter tables through the same identity/range/persistence path; partial or malformed tables safely remain whole-file rows. ADR-0032 persists typed stream/subsong selection separately from identity and range, enumerates tracker songs through libopenmpt, leaves alternate container streams opt-in, and carries selected songs through Trackbench and the gapless FFmpeg playback core. The bounded playback core proves transport, SPSC render, zero-fill underrun recovery, and exact consumed-sample position. The direct PipeWire adapter proves exact-rate mono/stereo float negotiation and an RT-safe render boundary. ADR-0021 serializes source replacement, scheduled decoder production, transport, seek, drain, quiescence, and cancellation on one dedicated worker; duration-based buffers scale at the actual source rate. ADR-0023 adds originating-list progression, ADR-0024 adds perceptual stream volume plus device selection, ADR-0029 adds persistent named buffer profiles plus bounded exact values without mutating a live RT ring, and ADR-0030 adds continuous sink/default monitoring with strict explicit-target pause/recovery. ADR-0062 now lets exact current, prepared-next, and queued-intent bindings follow file publication without decoder/output restart and compensates them on durable dependent-state failure. The superseded ADR-0022 domain-chip transport binding is removed during the split; this whole engine is Trackbench's first-class player. Device-clock projection and representative latency measurement remain future hardening. |
| Playback ownership | Separate applications | M4 | MPD remains shared server state controlled by multiple clients and advertised outputs/agents; the Trackknife client is purely its client. Local playback lives in Trackbench, a separate process with its own transport; there is no mixed queue, no backend handoff, and no cross-process side effects (ADR-0025). |

## Priority 2: Trackbench metadata, MusicBrainz, and ReplayGain

| Area | Capability | Milestone | Required behavior |
| --- | --- | --- | --- |
| Tagging | Fast field grid | M5 (tabbed draft workspace) | ADR-0034 provides the bounded sparse selection model, ADR-0035 adds direct scalar editing and bounded drafts, ADR-0036 establishes Fields/Original/Draft, and ADR-0037 adds exact ordered-value editing without delimiter parsing. ADR-0038 simplifies Properties to one file-selection-driven split: one selected file exposes exact values and several define bulk scope. ADR-0039 adds arbitrary field insertion/removal and ADR-0040 adds ranked open completion. ADR-0052 now hosts the complete Properties surface in a temporary protected `Tags · N tracks` workspace tab rather than a separately sized window; tagging tabs remain outside saved list documents and preserve draft/apply close protection. Saved field layouts remain. |
| Tagging | Multi-file states | M5 (native-FLAC Apply baseline) | Common, mixed, missing, and partial baseline states use exact ordered value vectors across selected occurrences. ADR-0041 projects exact Draft states, ADR-0042 rereads every staged physical source and previews structured blockers, and ADR-0043 makes compatible native-FLAC text plans ready while rejecting unrepresentable mappings. ADR-0044 proves serialized, journaled, atomically published and recoverable per-source commits, including all duplicate/CUE/chapter occurrence indexes; ADR-0045 atomically refreshes every persisted duplicate while retaining logical overlays. ADR-0046 adds startup recovery/reconciliation presentation plus bounded retained-backup undo/release. ADR-0047 now applies wholly ready plans with bounded cancellation, per-source partial results, and mandatory fresh-preview retry. |
| Tagging | Arbitrary/multi-value data | M5 (native-FLAC write proof) | Real FLAC/WavPack and offscreen restart tests preserve ordered repeated/custom/MusicBrainz values and native exposed names. ADR-0043's native-FLAC prepared-copy writer round-trips ordered arbitrary and MusicBrainz edits while preserving untouched ReplayGain text, an unknown APPLICATION payload, artwork, every other non-comment/non-padding block, compressed audio bytes, and decoded PCM. Other format writers remain unproven. |
| Tagging | Derived values/chains | M5 (matching/numbering slice) | ADR-0048 adds versioned ordered declarative chains over the current draft; ADR-0049 persists named definitions and exact append/copy/split/join alongside literal set, remove, per-value trim/lower/upper, and scalar `tkfmt-1`; ADR-0050 adds typed first-character Unicode capitalization with precise empty-preview diagnostics. ADR-0051 puts the complete saved catalog in a persistent checkable **Tagging scripts** side panel and opens the selected definition in the editor. Checked chains run in displayed deterministic order against a temporary copy of the manual draft before fresh revalidation: the final immutable write plan contains their exact results, repeated preview cannot compound them, and explicit Apply remains mandatory. ADR-0052 reduces one-off previews to expandable Field/Old/New rows while retaining affected-file and producing-step detail. ADR-0053 adds complete-value case-sensitive remove/replace actions and bounded consecutive numbering in captured file order; all three persist and traverse manual plus checked-automatic preview paths. Target entry still ranks fields present in the selected tags while accepting arbitrary names. Grouped numbering/`TOTALTRACKS`, richer match dialects, import/export, and the separate capture-pattern grammar remain. |
| Metadata | Provider proposals | M5 | Internal provider boundary returns proposed values, MusicBrainz identities, artwork references, provenance, and confidence into the staged preview. A public plugin ABI remains deferred. |
| Metadata | MusicBrainz identification | M6 | Online release/recording matching, identifier and artwork proposals with provenance/confidence, honest rate limiting/caching, explicit cancellable network operations, optional AcoustID evaluation. |
| Artwork | Management | M5 | Typed embedded/external art, replace/remove/export, preservation tests. |
| Files | Rename/move/copy | M5 (bounded publication core) | ADR-0054 places independent Save tags/Rename/Move/ReplayGain choices in one preparation-plan architecture. ADR-0055/migration 13 persist reusable layouts and destinations and provide the pure final-metadata path plan. ADR-0056/migration 14 add fresh no-symlink preflight and distinct same/cross-filesystem recovery evidence. ADR-0057 executes one locked same-filesystem rename with journal-before-mkdir, `renameat2` no-replace publication, parent syncing, exact rollback, idempotent callback replay, and conservative startup reconciliation. ADR-0059/migration 15 implements that callback as one revision-guarded all-occurrence list/cache relocation transaction, with ordered stale-snapshot convergence and real-file executor integration. ADR-0060/migration 16 adds a linked, retryable reverse publication with exact no-replace undo, dependent rollback, idempotency, and startup replay. ADR-0061 adds bounded ownership/permission/timestamp/xattr-preserving cross-filesystem preparation, full byte verification, no-replace target publication, dependent commit before exact source removal, rollback, and recovery across every durable copy boundary. ADR-0062 revision-qualifies current/gapless/queued local-player references, re-keys them without transport disruption, and reverses ephemeral audio state on durable callback failure. ADR-0063 applies one reviewed multi-source preflight with bounded same/cross-filesystem dispatch, fresh admission, shared-directory coordination, monotonic progress, cancellation, and ordered partial results. The tagging side panel now edits persisted naming and raw-path destination profiles through the serialized worker; Save tags is a functional choice, while Rename, Move, and M7 ReplayGain remain visibly capability-gated. Cross-filesystem undo, operation history, combined-content publication, and final preview/Apply UI wiring remain required before Rename or Move is exposed as usable. |
| Loudness | Universal parallel scan | M7 | Every decodable source; bounded FFmpeg/libebur128 jobs; track/album and peaks. |
| Loudness | Metadata integration | M7 | Review beside tags; embed safely or use explicit sidecar/database fallback. |
| Loudness | Playback application | M7 | Track/album modes, preamps, clipping prevention, source-aware behavior. |

## Priority 3: converter and import

| Capability | Milestone | Required behavior |
| --- | --- | --- |
| Codec presets | M8 | Versioned FFmpeg capabilities, useful defaults, missing encoder diagnostics. |
| Destination mirror | M8 | Preserve source structure relative to an explicit root. |
| Destination expressions | M8 | Presets or `tkfmt-1` relative paths with complete sanitization/conflict preview. |
| Signal pipeline | M8 | Resample/channel/bit depth/dither, optional DSP/gain, bounded parallelism. |
| Metadata/art transfer | M8 | Capability-aware mappings and warnings; no stale loudness after signal change. |
| Publication/verification | M8 | Temp sibling, atomic publish, cancellation cleanup, optional output scan/check. |
| Organized publication | M8 | Previewed copy/move/converted publication below a configured accessible root, containment revalidation, verification, and atomic commit. A destination may be an MPD music root reached by plain filesystem access; Trackbench speaks no MPD protocol, and requesting a database update afterwards is a job for any MPD client (ADR-0025). |

## Priority 4 and later

| Capability | Status | Notes |
| --- | --- | --- |
| Unified Trackbench MPD/local workspace | In progress | ADR-0058 accepts authority-bound MPD Queue and Local Queue tabs in one process. Active context switches transport, MPD/PipeWire outputs, server-library/local-folders sidebar, and MPD-only mode controls; local mutation tools remain unavailable in MPD context. The MPD tab now restores a splitter-bounded right-aligned live search whose transient surface dismisses on focus loss, renders one-line results with bounded square covers and visible keyboard-action focus, and supports append/add-next/replace plus continuation. The MPD library shares inline/context append-next-replace actions and exact-target queue drops; the live queue restores the priority/crop menu and exposes Home/End boundary navigation. Advertised ReplayGain displays its current mode beside the selector. MPD uses the exact complete local track-view layout schema, editor, presets, expressions, persistence, and renderer. Committed search/stored-playlist tabs and profile migration remain; the standalone MPD shell stays during that migration. |
| Melody playback endpoint | M9 | Register, stream/direct source, queue sync, preload, report clock/state. |
| MPRIS/media keys/notifications | M9 | Linux desktop integration after playback ownership is proven. |
| DSP graph | M9/later | Versioned presets; bypass must not alter samples. |
| Trackbench local index | Deferred | Add only if direct filesystem navigation plus working lists stop scaling. |
| Autoplaylists/deep query | Deferred | Server search and `tkfmt-1` sorting first. |
| Online MusicBrainz provider | M6 | First metadata-provider implementation; explicit network capability whose proposals still pass through normal preview/write safety. |
| Melody remote import | Proposal | No protocol extension exists today. If Melody later advertises transactional upload/import, implement it as an optional destination for the existing import plan. |
| Plugin API | Deferred | Derive from proven built-ins rather than freezing an early ABI. |
| CD ripping/internet radio | Deferred | Separate product and correctness designs. |

## Format-support dimensions

Never publish one undifferentiated “supported formats” list. Track local formats
independently:

| Dimension | Meaning |
| --- | --- |
| Probe/decode | Produce technical data/PCM for local playback, scanning, conversion, verification. |
| Seek/gapless | Accurate duration, sample seeking, delay/padding/segment behavior. |
| Read metadata | Text, arbitrary values, MusicBrainz, binary/art, chapters/cues, loudness. |
| Write metadata | Safely update intended data while retaining unknown structures and audio essence. |
| ReplayGain embed | Interoperable embedded mapping exists and is tested. |
| Sidecar loudness | Explicit fallback when embedding is unsafe/unavailable. |
| Convert output | Encoder/muxer exists with versioned options. |
| Verify | Decode/container/checksum behavior is specified. |

The initial local matrix should cover MP3, FLAC, Ogg Vorbis, Opus, MP4/M4A,
WavPack, WAV/RF64, AIFF, and common cue/subsong cases before expanding claims.
FFmpeg decoding alone never implies safe tag writing or exact gaplessness.
